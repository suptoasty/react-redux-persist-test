// import Counter from "@/features/counter/counter"
// import { mount } from "enzyme"

describe("Counter Component", () => {
	it.todo("Counter Renders")
	// it("Counter Renders", () => {
	// 	const wrapper = mount(<Counter />)
	// 	// expect(wrapper.count).toBe(0)
	// })
})
